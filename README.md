# ROI-ML-Prediction-Model
Built a predictive model using Python and scikit-learn to forecast Return on Investment (ROI) for marketing campaigns. The workflow includes data cleaning, feature engineering (CTR, ROI per cost), one-hot encoding, scaling, and regression models (Linear, Ridge, Lasso). Deployed via Flask for live prediction. l also used Streamlit for hosting
